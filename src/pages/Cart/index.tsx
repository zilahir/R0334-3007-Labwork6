import { ReactElement } from "react";
import Layout from "../../components/Layout";

const Cart = (): ReactElement => (
    <Layout>
        <p>
            hello
        </p>
    </Layout>
)

export default Cart;